# Core dependencies for CPU-optimized image restoration app

# Framework web
streamlit>=1.28.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# PyTorch CPU-only (no CUDA)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cpu

# Utilities
requests>=2.31.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Face detection and enhancement (simplified for CPU)
scikit-image>=0.21.0

# Additional image processing
scipy>=1.11.0
matplotlib>=3.7.0